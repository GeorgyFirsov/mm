use std::path::Path;

use crate::error::Result;


/// Runs built-in xi-based editor and waits for completion.
/// 
/// * `note_path` - full path to a note to edit
pub(super) fn run_editor(note_path: &Path) -> Result<()> {
    // TODO
    Ok(())
}
